# ── 开发侧文档（不随仓；仅本地参考，不入 git）────────────────
# 口径契约、交接日志与项目约定：给维护者/agent 看，不属于翻译工作流
SPEC.md
HANDOFF.md
AGENTS.md

# ── 只读输入（体量大 / 非本项目产出）──────────────────────
corpus/
reference/

# ── 中间产物（可重建）─────────────────────────────────────
build/

# ── Python ────────────────────────────────────────────────
__pycache__/
*.pyc
