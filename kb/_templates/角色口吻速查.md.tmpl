# 模板 · 罪人口吻速查表　**v1.0（冻结 2026-09-20）**

> **冻结说明**：与 `角色指南.md.tmpl` v1.0 同批冻结。量化总表**由脚本生成，禁止手工编辑数字**。
>
> **生成流程**
> 1. `python3 tools/voice.py --table` → 粘贴进「量化总表」
> 2. `python3 tools/voice.py --compare` → 粘贴进「敬称行占比排行」
> 3. 人工撰写「硬结论」「硬标记表」（每条必须指向表内数字/可回溯到对应角色指南）
> 4. `python3 tools/lint_kb.py kb/style/角色口吻速查.md` → **FAIL 0**
>
> **语料基准**：零协 Release `{{llc_version}}` × 游戏本体 `{kr,en,jp}`（抓取 {{snapshot_date}}）
> **口径依据**：`kb/口径.md` G1–G5、M1、M2、M4、E1、E2
> **权威等级**：`零协权威`

---

## 量化总表

> 全部列均为 **每百句密度（M4）**，唯「句数」为 M1、「均中字」为 M2。`……` 按 **1 次**计（口径 G5）。
> 表宽 = 12 列 + 2 列基础量。本表由 `python3 tools/voice.py --table` 直接产出。

| 角色 | 句数(M1) | 均中字(M2) | …… | ！ | ？ | 我 | 我们 | **你** | **您** | 呢 | 啊 | 吧 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| {{table_rows_from_voice_table}} |

> ⚠️ 本表**必须包含 `你`/`您` 两列**（部分现成口径只有 10 列）→ 这是本项目要修的第一个口径缺口（口径 §0）。

### 敬称行占比排行（E2-cn）

> 由 `python3 tools/voice.py --compare` 产出；lint 会逐行用对应角色的值复算。

| 排名 | 角色 | 敬称行占比（E2-cn） | 行数 | 主题语域（E1） |
|---:|---|---:|---:|---:|
| {{rank_rows_from_voice_compare}} |

### 可选扩展列（按需启用，默认关闭）

| 列 | 公式 | 用途 |
|---|---|---|
| 咱/俺/本人/在下/老夫/人家 | M4 | 独占性自称识别 |
| 呀/哦/哟/啦/嗯/哈/呜 | M4 | 语气词谱 |
| 均英词 | M3 | 句长对照 |

---

## 从此表可直接读出的硬结论

> 由 LLM 撰写，但**每条必须指向表内数字**（A2 断言保护）。

1. **{{角色}}**：{{最大偏离列}} = {{值}}（M4），是{{对比对象}}的 {{倍数}} 倍 → {{翻译指令}}
2. **{{角色}}**：全项最低（{{列}} {{值}}）→ {{翻译指令}}
3. **「您」的使用者**：{{列名单}}（M4）→ 这几位对上级/生人用敬称

---

## 使用方式

1. 用 `build/packs/<角色>.md` 确认说话人（剧情 JSON 的 `model` 字段是稳定键）
2. 打开 `kb/style/角色/<中文名>.md` 看完整规则与实例
3. 确认某个说法零协怎么翻 → `python3 tools/corpus.py grep '<中文>' -l cn`

---

## 各角色不可违反的硬标记（从 13 份指南提炼）

> 由 LLM 撰写；每条必须可回溯到对应角色指南的小节。

| 角色 | 自称 | 对但丁的称呼 | 标志性标点/口癖 | 绝对禁止 |
|---|---|---|---|---|
| {{cn_name}} | {{自称}} | {{对但丁}} | {{标点}} | {{禁止}} |

---

<!-- 生成后自查（lint_kb.py 强制，必须 FAIL 0）：
     [ ] A1 引用可解析：@@ 残留 0
     [ ] A2 数字可复算：量化总表 12 列 × 13 行、敬称排行 13 行，逐格 == voice.py 输出
     [ ] A3 硬标记表每条可回溯
     [ ] 硬结论每条指向具体列与数值
     [ ] 硬标记表 13 行齐全
-->
