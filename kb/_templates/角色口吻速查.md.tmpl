# 模板 · 罪人口吻速查表　**v1.1（冻结 2026-09-20）**

> **冻结说明**：与 `角色指南.md.tmpl` v1.0 同批冻结；**v1.1 修订三处**（尚无产物，零重跑成本）：
> ① 列数表述修正为「10 列（M4）+ 2 列基础量（M1/M2）」；② 硬标记表加「来源」列并让章节标题可被 A3 命中；
> ③ 新增「口径说明与语料陷阱」节。量化总表**由脚本生成，禁止手工编辑数字**。
>
> **生成流程**
> 1. `python3 tools/voice.py --table` → 粘贴进「量化总表」
> 2. `python3 tools/voice.py --compare` → 粘贴进「敬称行占比排行」
> 3. 人工撰写「硬结论」「硬标记表」「口径说明与语料陷阱」
>    （硬结论每条必须指向表内数字；硬标记表每行必须可回溯到对应角色指南的小节 + `文件#id`）
> 4. `python3 tools/lint_kb.py kb/style/角色口吻速查.md` → **FAIL 0**
>
> **语料基准**：零协 Release `{{llc_version}}` × 游戏本体 `{kr,en,jp}`（抓取 {{snapshot_date}}）
> **口径依据**：`kb/口径.md` G1–G5、M1、M2、M4、E1、E2
> **权威等级**：`零协权威`

---

## 量化总表

> 全部列均为 **每百句密度（M4）**，唯「句数」为 M1、「均中字」为 M2。`……` 按 **1 次**计（口径 G5）。
> 表宽 = **10 列（M4）+ 2 列基础量（M1/M2）**。本表由 `python3 tools/voice.py --table` 直接产出。

| 角色 | 句数(M1) | 均中字(M2) | …… | ！ | ？ | 我 | 我们 | **你** | **您** | 呢 | 啊 | 吧 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| {{table_rows_from_voice_table}} |

> ⚠️ 本表**必须包含 `你`/`您` 两列**（早期口径只统计了 10 个模式、漏掉这两列，复现时句数亦差 4.4%）。

### 敬称行占比排行（E2-cn）

> 由 `python3 tools/voice.py --compare` 产出；lint 会逐行用对应角色的值复算。

| 排名 | 角色 | 敬称行占比（E2-cn） | 行数（E2） | 主题语域（E1） |
|---:|---|---:|---:|---:|
| {{rank_rows_from_voice_compare}} |

### 可选扩展列（按需启用，默认关闭）

| 列 | 公式 | 用途 |
|---|---|---|
| 咱/俺/本人/在下/老夫/人家 | M4 | 独占性自称识别 |
| 呀/哦/哟/啦/嗯/哈/呜 | M4 | 语气词谱 |
| 均英词 | M3 | 句长对照 |

---

## 从此表可直接读出的硬结论

> 由 LLM 撰写，但**每条必须指向表内数字**（A2 断言保护：标注 `值（M#）` 会与同行角色名逐条复算）。

1. **{{角色}}**：{{最大偏离列}} = {{值}}（M4），是{{对比对象}}的 {{倍数}} 倍 → {{翻译指令}}
2. **{{角色}}**：全项最低（{{列}} {{值}}）→ {{翻译指令}}
3. **「您」的使用者**：{{列名单}}（M4）→ 这几位对上级/生人用敬称

---

## 使用方式

1. 用 `build/packs/<角色>.md` 确认说话人（剧情 JSON 的 `model` 字段是稳定键）
2. 打开 `kb/style/角色/<中文名>.md` 看完整规则与实例
3. 确认某个说法零协怎么翻 → `python3 tools/corpus.py grep '<中文>' -l cn`

---

## 各角色不可违反的硬标记（自称 / 称呼 / 口癖）

> 由 LLM 从 13 份角色指南提炼；每行必须可回溯到该角色指南的小节，且**至少带 1 个 `文件#id`**（A3 断言）。
> 本表若写数字，只能是「该角色指南已复算的指标」或「该角色的指标集内值」——lint 会按行复算。

| 角色 | 自称 | 对但丁的称呼 | 标志性标点/口癖 | 绝对禁止 | 来源 |
|---|---|---|---|---|---|
| {{cn_name}} | {{自称}} | {{对但丁}} | {{标点}} | {{禁止}} | `kb/style/角色/{{cn_name}}.md` §自称 / §称呼他人的方式 / §该角色容易翻错的点 |

---

## 口径说明与语料陷阱

> 写**本项目自己的口径**（不引任何外部来源）；只写会影响本表数字的项。

- **`……` 按连续串计 1 次**（口径 G5），故 `……` 列是「按 1 计」值，不是按字符计的 2 倍。
- **角色归属用基准 model**：剧情变体（如 `{{kr_model}}` 的派生 model）已由 `python3 tools/voice.py --list` 排除，不计入 `L`。
- **`StoryData/E001X.json`（愚人节身体互换章）**：该文件的 kr `model` 标的是**身体**而非说话人，故其中若干行实为他人语气；本项目**未排除**该文件，实测对 13 角色的影响 ≤ **1.91% 行**（可复核：`python3 tools/voice.py --role <角色>` 与 `corpus.py show StoryData/E001X.json`）。
- **`L` 与 `L_text`**：本表「句数」用 `L`（M1）；角色指南里的 `L_text` 仅用于 M2/M3 对账，两者不可混用。

---

<!-- 生成后自查（lint_kb.py 强制，必须 FAIL 0）：
     [ ] A1 引用可解析：@@ 残留 0
     [ ] A2 数字可复算：量化总表 10 列（M4）+ 2 列基础量 × 13 行、敬称排行 13 行，逐格 == voice.py 输出
     [ ] A2 硬结论 / 硬标记表里的每个 `值（M#）` 与同行角色名一致；派生值写成算式
     [ ] A3 硬标记表每行带来源列 + 至少 1 个 `文件#id`
     [ ] 硬标记表 13 行齐全
-->
